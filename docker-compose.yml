version: '3.8'
services:
  app:
    environment:
      TZ: ${TZ}
      REDIS_HOST: redis
    env_file:
      - .env
    build:
      context: ./
    links:
      - redis
  redis:
    image: redis
    volumes:
      - redis_data:/data
volumes:
  redis_data:
